@page
@model HelpDesk.Pages.EmployeeTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeType[0].EmployeeTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EmployeeType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeTypeName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EmployeeTypeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EmployeeTypeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EmployeeTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
