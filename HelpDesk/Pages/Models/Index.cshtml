@page
@model HelpDesk.Pages.Models.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Model[0].ModelName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model[0].Model_BrandID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model[0].Model_CategoryID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModelName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model_BrandID.BrandID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model_CategoryID.CategoryID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ModelID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ModelID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ModelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
